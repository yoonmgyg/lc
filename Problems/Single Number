class Solution {
public:
    int singleNumber(vector<int>& nums) { 
        int ans = 0;
        for (auto x : nums) { // loop through nums
            ans = ans ^ nums[i]; // XOR answer so that duplicates cancel out and single out number
        }
        return ans;
    }
};
