class Solution {
public:
    int missingNumber(vector<int>& nums) {
        int len = nums.size();
        for (int i = 0; i < nums.size(); i++) // loop through nums
            len += i - nums[i]; // adding 0 to n - 1, subtracting each index will result in summing to the missing number
        return lens
    }
};
